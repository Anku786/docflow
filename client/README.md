# DocFlow

React + TypeScript + Vite app for reviewing extracted documents. Converted from the DocFlow HTML prototype: dashboard, search/filter, and an invoice review screen.

```bash
npm install
npm run dev
```
